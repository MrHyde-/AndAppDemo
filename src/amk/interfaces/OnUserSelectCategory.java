package amk.interfaces;

/*
 * This is used when user selects categories (the stars on the main activity) 
 * 
 */

public interface OnUserSelectCategory {
	public void onItemSelected(int category);
}
